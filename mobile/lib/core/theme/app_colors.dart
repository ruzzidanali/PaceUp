import 'package:flutter/material.dart';

abstract final class PaceUpColors {
  // Dark theme
  static const darkBackground = Color(0xFF080A0D);
  static const darkPanel = Color(0xFF11161B);
  static const darkPanelSecondary = Color(0xFF1A2026);
  static const darkText = Color(0xFFF6F8F9);
  static const darkMuted = Color(0xFF96A0AB);
  static const darkBorder = Color(0xFF283038);

  // Light theme
  static const lightBackground = Color(0xFFF5F7F8);
  static const lightPanel = Color(0xFFFFFFFF);
  static const lightPanelSecondary = Color(0xFFE8EDF0);
  static const lightText = Color(0xFF101318);
  static const lightMuted = Color(0xFF58636E);
  static const lightBorder = Color(0xFFD7DFE4);

  // Brand / sport accents
  static const electricGreen = Color(0xFFB8FF3D);
  static const greenInk = Color(0xFF122000);

  static const electricCyan = Color(0xFF27D7FF);
  static const lightCyan = Color(0xFF007D98);

  static const danger = Color(0xFFFE645C);
}
